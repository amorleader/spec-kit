spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/speckit_mhr
    username: mhr_user
    password: t!uL^rSxYETsXmiaa&yW
    driver-class-name: org.postgresql.Driver

app:
  ai:
    mode: ${AI_MODE:mock}
    base-url: ${AI_BASE_URL:}
    api-key: ${AI_API_KEY:}
    model: ${AI_MODEL:gpt-4o-mini}
    timeout-seconds: ${AI_TIMEOUT_SECONDS:90}
    proxy-enabled: ${AI_PROXY_ENABLED:false}
    proxy-host: ${AI_PROXY_HOST:127.0.0.1}
    proxy-port: ${AI_PROXY_PORT:7890}
  spec-kit:
    execution:
      timeout-seconds: ${SPEC_KIT_EXEC_TIMEOUT_SECONDS:120}
      openhands-enabled: ${SPEC_KIT_OPENHANDS_ENABLED:false}
      openhands-base-url: ${SPEC_KIT_OPENHANDS_BASE_URL:}
      openhands-api-key: ${SPEC_KIT_OPENHANDS_API_KEY:}
  mhr:
    catalog:
      repository-mode: db
      import-on-startup: false
      import-json-path: ""
  expense:
    repository-mode: db
